[internal]

exten => _10XX,1,NoOp(Call to ${EXTEN})
 same => n,Dial(PJSIP/${EXTEN},30)
 same => n,Hangup()

exten => 600,1,Answer()
 same => n,Playback(demo-congrats)
 same => n,Hangup()

exten => 700,1,Answer()
 same => n,Echo()
 same => n,Hangup()